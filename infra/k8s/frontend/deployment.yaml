apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment 
  namespace: ld
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ld
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  template:
    metadata:
      labels:
        app: ld
    spec:
      containers:
        - name: frontend-cont
          image: chandu554/frontend-lb-service:latest
          ports:
            - containerPort: 5173
          env:
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: attendance-secret
                  key: MONGO_URI
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: attendance-config
                  key: PORT
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "200m"
              memory: "512Mi"